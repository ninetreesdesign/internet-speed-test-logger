# macOS
.DS_Store
.AppleDouble

# Dropbox
.dropbox
desktop.ini
Icon?

# CSV log exports (large data files, managed outside git)
speedtest_logs/
speedtest_prefs.txt

# LiveCode companion app prefs (not part of HTML app)
speedtest_prefs.txt

# Background collector log (written by launchd agent)
speedmon_bg.log
